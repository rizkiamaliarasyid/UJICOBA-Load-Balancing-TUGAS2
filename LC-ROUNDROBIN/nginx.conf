events { worker_connections 1024; }

http {
    upstream opsi_server {
        #least_conn;
        server web-1:5000;
        server web-2:5000;
        server web-3:5000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://opsi_server;
        }
    }
}

